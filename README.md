# intelligent_document_classification

## Overview

This project aims to compare the performance of several neural network architecures performance on a dataset of documents images. The models are trained to classify data by computing a predicted class among the 16 documents category defined by the RVL-CDIP dataset. This dataset is also used to train the models and compute performance metrics.
